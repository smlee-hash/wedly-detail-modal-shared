# 세 앱 상세창 "공용 틀 하나로 통합" 설계

작성일: 2026-05-30 · 대상: 하이브 / 일루아 / ERP(5개 모듈) · 공용 부품: `@wedly/detail-modal-shared`

## 1. 목표 (사장님 지시)

세 앱(하이브·일루아·ERP)의 상세창을 **하이브 구조의 공용 틀 하나**로 통일한다.
앱마다 다른 것은 다음뿐이어야 한다:

1. **컬럼 이름** (필드 라벨·키)
2. **컬럼 순서·위치** (어느 섹션/탭에 어떤 순서로)
3. **켤 탭 선택** (예: 일루아는 회의 안 켬)
4. **앱별 서버 연결** (댓글·이력·데이터 저장 위치 — 앱마다 서버가 다름)

"틀(겉 모양·탭 배치·동작)"은 100% 공용, 위 1~4만 앱별 설정.

## 2. 현재 상태 (실제 코드 확인 결과 — 추측 아님)

- **공유되는 건 탭 조각뿐**: 정산·회의·파일 탭은 공용 부품을 같이 쓴다. **상세창 전체 틀은 공유 안 됨.**
- **손으로 짠 틀 파일이 7개**:
  - 하이브 1개 (3,225줄) — 이미 `sections`·`customPanels`·다수 콜백을 *넘겨받는 값*으로 처리하는 **가장 진화된 거의-공용 형태**
  - 일루아 1개 (1,688줄) — **회의 탭 없음**, 댓글은 이력 안에 인라인
  - ERP 5개 (904~2,097줄) — 서로 **복붙 수준**, 탭이 한 줄에 평면 배치
- **공용 폴더에 미사용 초안 1개** (2,598줄) = **하이브 상세창을 복사해 둔 것** (경정 키는 하이브 자체 도메인 키). 일반화·내보내기(export) 안 돼 아무도 안 씀.
- **의존 방식**: 세 앱 모두 `github:smlee-hash/wedly-detail-modal-shared`. → 공용 부품을 고쳐 올리면, 각 앱이 **다시 설치할 때** 반영(자동 아님).

## 3. 핵심 통찰

하이브 상세창은 **이미 거의 공용 틀**이다(섹션 목록·패널·여러 동작을 입력값으로 받음). 아직 하이브 안에 박혀 있어 떼어내야 할 것은 **딱 4가지**:

| 박혀 있는 것 | 현재 | 공용화 방법 |
|---|---|---|
| ① 컬럼 정의 | `CONTRACT_FIELDS` 상수가 코드에 박힘 | **넘겨받는 값(prop)** 으로 |
| ② 권한 | `useAccess()` 직접 호출(하이브 전용) | `isAdmin` **넘겨받는 값** 으로 |
| ③ 서버 주소 | `/api/entries/...`, `/api/detail-section-mapping/...` 등 고정 | **데이터 연결 어댑터** 로 주입 |
| ④ 댓글·이력 | `HistoryPanel`(하이브 전용) + 고정 댓글 주소 | **이력 연결 어댑터**(불러오기/쓰기/삭제 + 카테고리) 로 주입 |

이 4가지만 "앱이 넘겨주는 값"으로 바꾸면 하이브 틀이 그대로 공용 틀이 된다.

## 4. 설계 — 공용 틀이 받는 "설정 표면"

공용 `DetailModal`(새로 export)이 받는 값:

1. `fields` — 컬럼 정의(이름·타입·포맷·키). **[앱별]**
2. `sections` — 섹션 목록 + 순서 + `fieldKeys`(컬럼 위치) + `kind`(fields/settlement/meetings/files/tiered-*). **[앱별]** *(이미 존재)*
3. `isAdmin` — 권한. **[주입]**
4. `dataSource` — 데이터 연결 어댑터: row 저장(patch)·신규 생성·파일 업로드/다운로드 경로·차수필드 읽기/쓰기·섹션매핑 읽기/쓰기. **[앱별]**
5. `historySource` — 이력·댓글 연결 어댑터: 불러오기/등록/삭제 콜백 + 카테고리 정의. (또는 이력 패널을 통째로 넘기는 방식) **[앱별]**
6. 탭 렌더링은 `kind` 별로 이미 공용 탭(정산·회의·파일) 사용.

→ **틀·탭 배치·동작은 100% 공용**, 위 1~5만 앱별. ERP 5개 모듈의 복붙 틀(합계 7천여 줄)이 **작은 설정 파일 5개 + 공용 틀 1개**로 줄어드는 게 진짜 이득.

## 5. 안전 계약 (절대 안 깨뜨릴 것)

- **정산(돈) 탭**: 이미 공용 — 저장 방식·계산 **그대로**.
- **파일 저장**: 칸별 저장 방식 **그대로**(기존 `ErpFilesPanel` 어댑터 유지) → 일루아 동기화·외부 연동·이력·보정 무영향.
- **댓글·이력**: 앱별 서버에 **그대로 저장**(노션 / DB / REST). 틀만 공용, 연결은 앱별 어댑터로.
- **데이터 읽기/쓰기 경로**: 각 앱 기존 경로 **변경 없음** — 어댑터로 그대로 호출.
- 하이브·일루아·ERP **공용 부품 코드는 신중히** — 한 곳 고치면 세 앱에 영향. 단계마다 빌드·검증.

## 6. 단계적 적용 (안전 최우선 — 한 단계씩 검증 후 다음)

- **0단계**: 이 설계서 검토·승인.
- **1단계 (시범 = 하이브 자기 자신)**: 하이브 상세창을 공용 폴더로 옮겨 일반화·export. 하이브가 그 공용 틀을 가져다 쓰되 *자기 설정*을 넘김. → **하이브 화면 변화 0**(자기 코드라 동작 동일) = 추출 성공 증명. **가장 안전한 첫 단계.** 미사용 초안(2,598줄)은 이 일반화 버전으로 교체(삭제).
- **2단계 (ERP 1개 모듈 시범)**: 댓글이 가장 단순한 모듈 1개(**free-subsidy**, REST 댓글, 904줄)를 공용 틀로 교체. 탭 전환·저장·댓글·파일 검증.
- **3단계**: 나머지 ERP 4개(government·policy·tax·labor) + 일루아 순차 교체. 일루아는 회의 안 켬.
- 각 단계: 빌드 → 테스트 → 화면 동작 점검 → 커밋 → 푸시 → (해당 시) 공용 부품 새 버전 → 앱 재설치 → 배포 SUCCESS 확인 → PR. **회귀 시 직전 단계로 복원.**

## 7. 위험·결정사항

- **ERP 탭 배치 변경**: 평면 배치 → 하이브식(상세정보 안에 접어 넣기). 보이는 모습 변함. **사장님 승인 완료.**
- **댓글·이력 서버 차이**(노션 polling / `_comments` DB / REST): 어댑터로 흡수 — **가장 손 많이 가고 위험한 부분.** 2·3단계에서 모듈별로 신중히.
- **공용 부품 = github 의존**: 한 곳 고쳐 올려도 각 앱이 다시 설치해야 반영(자동 아님). 단계마다 재설치 확인.
- **일루아 차이**(회의 없음, 이력 카테고리 4종): "켤 탭 선택" + 카테고리 설정으로 흡수.

## 8. YAGNI (안 할 것)

- 모든 앱 댓글을 한 서버로 통합 — **안 함**(위험·범위 밖). 틀만 통일, 저장 위치는 앱별 유지.
- 미사용 초안 2,598줄 살리기 — 1단계 일반화 버전으로 **교체(삭제)**.
- 도메인 로직(정산 계산·환불 차수·파일 분류) 손대기 — **안 함**. 그대로 둠.

## 9. 검증 방법

- 단위 테스트: 파일 어댑터(이미 64개) + 새 "데이터/이력 연결 어댑터" 변환 로직.
- 단계별 빌드 + 화면 동작 점검(탭 전환·필드 저장·댓글 등록·파일 업로드/삭제).
- 1단계는 "하이브 동작 0 변화"가 합격 기준.

## 10. 추가 발견 — 실제 코드 정밀 조사 (2026-05-30, 1단계 착수 직전)

설계서 §3의 "박혀 있는 것 4가지"는 **상세창 본체 기준**이었다. 본체를 공용 폴더로 옮기려고
실제 코드를 끝까지 따라가 보니, 본체가 부르는 **하이브 고유 부속들도 같이 따라와야** 하고,
그 부속들이 *각자 또* 권한·확인창·서버주소를 물고 있었다(전이 의존). 옮길 묶음 = 8개 파일.

### 10-1. 미사용 초안은 버린다 (결정 확정 — 증거 기반)
공용 폴더의 미사용 초안(`src/components/DetailModal.tsx`, 2,598줄)은 하이브와 동작 일치도가
약 40~45%뿐(차수 카드 약 600줄·어드민 섹션/패널 약 300줄·히스토리 카테고리 약 200줄 누락),
권한 부품을 못 들여와 컴파일도 안 되며, 브라우저 기본 알림창을 쓴다. → **하이브 최신본에서
새로 일반화**가 일도 적고 "하이브 동작 0" 보장이 쉽다. 초안과 그 미등록 보조파일은 일반화 후 삭제.

### 10-2. 옮길 파일 묶음(전이 의존 닫힘) + 처리 방법

| 파일(하이브) | 성격 | 공용에서 처리 | 상태 |
|---|---|---|---|
| `detail-types.ts` | 순수 타입 | 그대로 복사 (detail-modal-shell/) | ✅ 1단계-A 완료 |
| `lib/person-id.ts` | 순수 함수 | 그대로 복사 (lib/) | ✅ 1단계-A 완료 |
| `lib/expired-url.ts` | 순수 함수 | 그대로 복사 (lib/) | ✅ 1단계-A 완료 |
| `_components/options.ts` | 데이터 헬퍼 | 공용에 이미 동일본 존재 → 등록 | ✅ 1단계-A 완료 |
| `_components/utils.ts` | 데이터 헬퍼 | 공용본에 날짜 가드 3줄만 동기화 | ✅ 1단계-A 완료 |
| `lib/open-file-with-refresh.ts` | 파일 열기 | 서버주소 2개를 `refetchEntryUrl`·`notionRefreshUrl` 입력값으로(미지정 시 하이브/ERP 기본 경로), 안내문구 `expiredMessage` | ✅ 1단계-B 완료 |
| `_components/FieldEditors.tsx` | 셀·필드 편집기 | 박힌 4가지를 입력값으로: 권한(`isAdmin`)·확인창(`dialog`=ShellDialog 묶음 모양)·파일열기(`openFile`)·드롭다운(`SelectDropdownBody`) | ✅ 1단계-B 완료 |
| `_components/DetailModal.tsx` | 본체 | 공용 틀(DetailModalShell)로 일반화 — config.ts 약속대로 | ⏳ 1단계-C |
| `_components/HistoryPanel.tsx` | 댓글·이력 | **옮기지 않음** — `renderHistoryPanel`로 통째 주입 | (주입) |

### 10-3. config.ts(설정 약속)에 보강 필요 — 본체(1단계-C) 작업 시 반영
- `dataSource.tieredFieldsPath?(prefix)` — 공용 정산탭이 이미 path 문자열로 차수 데이터를
  받으므로(하이브 래퍼가 그렇게 씀), 함수형 read/write 대신 path 생성기로 단순화 검토.
- `dataSource.bulkMigrateTier?` — 어드민 차수 일괄 이전(`/api/entries/bulk-migrate-tier`).
- FieldEditors가 파일 열기에 쓰는 두 주소를 dataSource로 흡수(open-file 주입 경로).

### 10-4. 하이브 정산탭 = 본보기 (실제 확인)
하이브 로컬 `SettlementInfoTab.tsx`(1.6KB)는 공용 정산탭을 도메인 값(비율 키·`configApiPath`·
기본카드·꼬리표)만 주입해 감싸는 얇은 래퍼였다. 상세창 감싸개도 정확히 이 방식을 따른다.

### 10-5. 안전 원칙 재확인
모든 작업은 **공용 폴더 안에서만**(복사·신규) 한다. 하이브 원본 파일은 4단계(감싸개 교체)
전까지 **건드리지 않는다** → 그때까지 운영 위험 0. 공용 폴더의 미등록 보조파일(options/utils)은
실제 쓰이는 공용 부품(정산·미팅·파일·드롭다운 탭)이 부르지 않으므로 운영 영향 없음(조사 확인).

### 10-6. 1단계-B 결과 — 편집기 주입 약속 + 1단계-D(하이브 감싸개)가 맞춰줄 것

공용 `EditableFieldRow` 가 새로 받는 입력값(예전엔 하이브에 박혀 있던 것):

| 입력값 | 타입 | 하이브 감싸개가 넘길 것 |
|---|---|---|
| `isAdmin?` | boolean | `useAccess().isAdmin` (없으면 false) |
| `dialog` (필수) | `ShellDialog` | 하이브 `useWedlyDialog()` 를 **묶음 모양으로 변환**해서 |
| `openFile` (필수) | `OpenFileFn` | `openFileWithRefresh` (기본 경로 그대로면 그대로 전달) |
| `SelectDropdownBody` (필수) | 컴포넌트 | 하이브 `HiveSelectDropdownBody` 그대로 |

**⚠️ 확인창 모양 차이 (1단계-D 에서 반드시 변환):** 하이브 `useWedlyDialog()` 는
`confirm(메시지, {title, danger})` / `alert(메시지, {title})` 처럼 **메시지를 앞**에 받지만,
공용 `ShellDialog` 는 `confirm({title, message, danger})` / `alert({title, message})` 처럼 **묶음**으로 받는다.
→ 하이브 감싸개에서 얇은 어댑터로 변환:
```ts
const dialog: ShellDialog = {
  confirm: ({ title, message, danger }) => hiveDialog.confirm(message ?? "", { title, danger }),
  alert:   ({ title, message })         => hiveDialog.alert(message ?? "", { title }),
};
```
이 변환만 해주면 파일 제거 확인·만료 안내가 예전과 100% 동일하게 뜬다(동작 0 변화).

**`dialog` 를 필수로 둔 이유:** 파일 제거는 확인 없이 삭제하면 안 되므로(브라우저 기본 confirm 금지 규칙),
편집기가 확인창을 반드시 받도록 강제 → 본체(1단계-C)는 파일 컬럼이 있으면 dialog 를 항상 넘겨야 함.

**블라스트 반경(1단계-D 주의):** 하이브에서 `EditableFieldRow`·`SelectEditor` 는 상세 모달뿐 아니라
**표 셀**도 같이 쓴다(AGENTS.md §5-4). 갈아끼울 때 두 호출 지점 모두 위 입력값을 넘겨야 한다(컴파일러가 누락을 잡아줌 — 그래서 props 주입 채택, context 아님).
